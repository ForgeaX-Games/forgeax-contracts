# @forgeax/types

Shared ForgeaX Zod schemas and TypeScript contracts. The published package is ESM and includes JavaScript plus matching declarations for every documented module export.

```ts
import { ExtensionManifestSchema } from '@forgeax/types/manifest';
```
